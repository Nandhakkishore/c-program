
#include <stdio.h>

int main() {
  int a,b,c,d;
  scanf("%d %d %d",&a,&b,&c);
 
  if(a==b && b==c){
      printf("Equivalent triangle");
  }
  else {
      printf("Not equivalent triangle");
  }
    

    return 0;
}